let number = prompt("Ведіть число:");
number = parseInt(number);
if(!IsNan(number)) {
if(number % 2 === 0 ) {
   alert("Число парне");
   } else {
   alert("Число непарне"); {
   }
   } else {
alert("Введене значення не є числом");
    }
    }
